# AGENTS

Read and follow this file strictly.

## Required verification
- docker compose build
- docker compose up -d
- pytest
- npm run build
- run linters/formatters if configured

## Secrets
If any command needs secrets, request .env values using a short checklist,
then stop until they are provided.

## Git workflow
- Work on the current feature branch.
- Commit and push changes before running tests.
